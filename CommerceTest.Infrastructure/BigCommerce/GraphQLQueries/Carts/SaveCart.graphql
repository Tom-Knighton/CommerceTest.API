mutation SaveCart($cartId: String!) {
    cart {
        createCartRedirectUrls(input: {cartEntityId: $cartId}) {
            redirectUrls {
                embeddedCheckoutUrl
                redirectedCheckoutUrl
            }
        }
    }
}