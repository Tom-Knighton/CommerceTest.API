query QueryProducts($query: String!) {
    site {
        search {
            searchProducts(filters: { searchTerm: $query }) {
                products {
                    edges {
                        node {
                            ...ProductFields
                        }
                    }
                }
            }
        }
    }
}
